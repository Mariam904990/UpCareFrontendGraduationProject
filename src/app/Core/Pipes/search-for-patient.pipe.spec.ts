import { SearchForPatientPipe } from './search-for-patient.pipe';

describe('SearchForPatientPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchForPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
