import { SearchBillPipe } from './search-bill.pipe';

describe('SearchBillPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBillPipe();
    expect(pipe).toBeTruthy();
  });
});
