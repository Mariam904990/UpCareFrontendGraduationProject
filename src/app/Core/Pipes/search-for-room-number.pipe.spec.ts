import { SearchForRoomNumberPipe } from './search-for-room-number.pipe';

describe('SearchForRoomNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchForRoomNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
