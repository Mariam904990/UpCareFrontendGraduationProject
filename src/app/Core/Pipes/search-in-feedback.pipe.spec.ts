import { SearchInFeedbackPipe } from './search-in-feedback.pipe';

describe('SearchInFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
